<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></a><a class="brand" href=<%=root_path %>>TrackIt</a><div class="nav-collapse">
            <ul class="nav navbar-right">
                <% if signed_in? %>
                <p class = "navbar-text"> Welcome back, <%= current_user.name %>.</p>
                <button type = "button" class ="btn btn-default navbar-btn"><%=link_to "Log out", destroy_user_session_path, method: "delete" %></button>
                <% else %>
                <li><%= link_to "Sign in", signin_path %></li>
                <li><%= link_to "Sign up", signup_path %></li>
                <% end %>
            </ul>
        </div>
    </div>
</div>