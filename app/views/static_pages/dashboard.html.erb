<div class="container">

    <div class="jumbotron hero-spacer">
        <h1>Get Thrifting!</h1>
        <p style="text-align:center">Paste the URL of the product you want to track below and we'll notify you when it goes on sale.</p>
        <h2 class="form-header">Product URL:</h2>
          <%= bootstrap_form_for(@tracker) do |f| %>
            <%= render 'shared/error_messages', object: f.object %>
            <%= f.text_field :url, autofocus: true, hide_label: true %>
            <div class="form-submit">
              <%= f.submit "start thrifting", class: "btn btn-outline-default btn-outline-inverted center" %>
            </div>
          <% end %>
    </div>
    <hr>
    <div class="row">
        <div class="col-lg-12">
            <h3>Your Tracked Items 
              <%= text_field_tag "search", nil, id: "search-tracker" %>
             <%= submit_tag "search", :name => nil, id: "search-trackers" %> </h3>
        </div>
    </div>
    <!-- /.row -->
    <div class="row text-center">
      <div class="dashboard-results">
        <% if @tracker_list.any? %>
          <ul class="trackers">
            <% # should be smart enough to automatically render the correct template --> _tracker.html.erb %>
            <%= render @tracker_list %>
          </ul>
        <% end %>
      </div>

    </div>
    <!-- /.row -->

    <hr>

</div>
    <!-- /.container -->
